{% extends 'admin/base.html.twig' %}

{% block css %}
    {{ encore_entry_link_tags('admin_form') }}
    {{ encore_entry_link_tags('admin_add_edit_nomination_lists') }}
{% endblock %}

{% set page_title = 'Změna listin' %}

{% block heading %}{{ page_title }}{% endblock %}

{% block main %}

    <h2 class="h4">{{ official.name }}</h2>

    <p>
        Zde je možné změnit již zadané listiny daného rozhodčího. (Listiny jsou řazeny vzestupně podle roku.)
    </p>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary btn-save" value="Uložit"/>
    {{ form_end(form) }}

{% endblock %}
