{% extends 'admin/base.html.twig' %}

{% set page_title = 'Listiny' %}

{% block heading %}{{ page_title }}{% endblock %}

{% block main %}

    <p>
        Zde je možné přidat k&nbsp;jednotlivým rozhodčím listinu, na kterou byli v&nbsp;dané půlsezóně umístěni.<br>
        Nejprve zadejte požadovanou sezónu a její část.
    </p>

    <div class="pb-5 text-width border-bottom">
        {{ form_start(seasonForm) }}
        {{ form_widget(seasonForm) }}
        <div class="row">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <input type="submit" class="btn btn-primary" value="Potvrdit"/>
            </div>
        </div>
        {{ form_end(seasonForm) }}
    </div>

    <h2 class="h4 pt-4">Změna listin</h2>
    <p>
        Pokud potřebujete změnit u některého rozhodčího jeho přiřazené listiny, vyberte jej ze seznamu níže.
    </p>

    {{ form_start(officialForm) }}
    {{ form_widget(officialForm) }}
    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" class="btn btn-primary" value="Ukázat listiny"/>
        </div>
    </div>
    {{ form_end(officialForm) }}

{% endblock %}
