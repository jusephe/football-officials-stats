{% extends 'admin/base.html.twig' %}

{% if page_title is not defined %}
    {% set page_title = 'Editace soutěže' %}
{% endif %}

{% block heading %}{{ page_title }}{% endblock %}

{% block main %}

    {% if create is not defined %}
        <h2 class="h4 mb-3">{{ league.fullname }}</h2>
    {% endif %}

    <div class="pt-2 pt-md-3">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="row">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <input type="submit" class="btn btn-primary px-4" value="Uložit"/>
            </div>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
