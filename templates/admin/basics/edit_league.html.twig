{% extends 'admin/base.html.twig' %}

{% block css %}<link rel="stylesheet" type="text/css" href="{{ asset('styles/admin/form.css') }}">{% endblock %}

{% if page_title is not defined %}
    {% set page_title = 'Editace soutěže ' ~ league.fullname %}
{% endif %}

{% block heading %}{{ page_title }}{% endblock %}

{% block main %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary btn-save" value="Uložit"/>
    {{ form_end(form) }}

{% endblock %}
