{% extends 'admin/base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block css %}<link rel="stylesheet" type="text/css" href="{{ asset('styles/admin/add_game.css') }}">{% endblock %}

{% set page_title = 'Přidat zápas' %}

{% block heading %}{{ page_title }}{% endblock %}

{% block main %}

    <div class="loading d-none">
        <div class="loading-align">
            <div class="loading-animation"></div>
            <div>Aktualizace statistik...</div>
            <p class="text-muted">
                Operace může chvíli trvat, mějte prosím strpení.
            </p>
        </div>
    </div>

    <div class="mb-3 mb-md-4">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary btn-send" value="Odeslat"/>
        {{ form_end(form) }}
    </div>

    <div class="mb-2 mb-md-3">
        <a href="{{ path('add_game_form') }}">Přidat zápas ručně</a>
    </div>

    <div class="mb-3 mb-md-4">
        <a href="{{ path('games') }}">Seznam přidaných zápasů</a>
    </div>

    <p>
        Na této stránce je možné vložit data ze zápasů. Do pole výše vložte zdrojový HTML kód stránky se zápisem
        o&nbsp;utkání z&nbsp;portálu
        <a href="https://is.fotbal.cz/" target="_blank" rel="noopener" aria-describedby="new-window">IS FAČR
            <svg class="icon" focusable="false" aria-hidden="true"> <use href="{{ asset('images/icons_sprite.svg') }}#new-window"></use> </svg></a>.
        Po vložení jednoho či více celých kol je potřeba aktualizovat tabulky se statistikami pomocí tlačítka níže.
    </p>

    <button type="button" class="btn btn-primary mb-3 mb-md-4" id="update_stats" data-path="{{ path('update_stats') }}">
        Aktualizovat statistiky
    </button>

    <div id="new_season" class="alert alert-warning">Na začátku nové sezóny je potřeba zkontrolovat, jestli některý tým nepozměnil
        svůj název (který se objevuje v&nbsp;zápisech) a případně ho aktualizovat na stránce <a href="{{ path('teams') }}">Týmy</a>.
    </div>

{% endblock %}

{% block scripts %}
    <script src="{{ asset('scripts/admin/add_game.js') }}"></script>
{% endblock %}
