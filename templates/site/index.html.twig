{% extends 'site/base.html.twig' %}

{% block main %}

    <p class="mb-4">
        Vítejte na stránkách projektu Rozhodčí Pražského fotbalového svazu, který poskytuje detailní statistiky o&nbsp;rozhodčích a delegátech PFS.
    </p>

    <h2 class="h4">Novinky</h2>

    <div class="table-responsive">
        <table class="table">
            <tbody>
            {% for post in posts %}
                <tr>
                    <td><a href="{{ path('post', {'id': post.id}) }}">{{ post.title }}</a></td>
                    <td class="text-right text-nowrap">{{ post.published|date("j. n. Y") }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
